services:
  - type: web
    name: dota-spy
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18
    healthCheckPath: /
    autoDeploy: true
